//! Device configuration (deprecated - moved to platform module).
//!
//! Device addresses are now defined in the platform-specific configurations.
//! See `platform::orangepi5::config` and `platform::qemu_virt::config`.
